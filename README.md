# 🚀 FastAPI CoreAgent Project

This project contains the implementation of the `coreAgent` and its components under:

- `core/agents`
- `memory/`
- `protocols/`
- `tools/`

It is built using [FastAPI](https://fastapi.tiangolo.com/) and serves as a modular AI agent framework.

---

## 📦 Requirements

- Python 3.8+
- pip (Python package manager)
- `requirements.txt` file with all necessary dependencies

---

## 🛠️ Setup & Run Instructions



